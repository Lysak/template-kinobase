# https://github.com/guard/guard-livereload
guard :livereload do
  watch(%r{.+.(css|js|html)$})
end
1
2
3
4
# https://github.com/guard/guard-livereload
guard :livereload do
  watch(%r{.+.(css|js|html)$})
end
